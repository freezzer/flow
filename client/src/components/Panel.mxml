<?xml version="1.0" encoding="utf-8"?>
<mx:Container xmlns:mx="http://www.adobe.com/2006/mxml"  width="100%" height="100%" xmlns:ns1="components.*">

    <mx:Script> <![CDATA[
        import mx.collections.ArrayList;
        import components.AsData;
        import components.EditField;
        
        private var val:components.AsData;
        private var path:String;
        private var fields:ArrayList;

        public function getPath():String {
            return path;
        }

        public function setPath(val:String):void {
            path = val;
        }


        public function getData():AsData {
            return val;
        }

        public function setData(val:AsData):void {
            this.val = val;
            var i:int;
            var len:int=fields.length;
            var f:EditField=null;
            for(i=0; i<len; i++){
               f=fields.getItemAt(i) as EditField;
               f.setValue( data.getValue(f.path) );
            }
            
        }

        public function addField(f:EditField):void {
            fields.addItem(f);
        }

        public function getField(path:String):EditField {
            var i:int;
            var len:int=fields.length;
            var f:EditField=null;
            for(i=0; i<len; i++){
               f=fields.getItemAt(i) as EditField;
               if(f.path==path) return f;
            }
            return null;
        }

        ]]></mx:Script>
        




</mx:Container>
